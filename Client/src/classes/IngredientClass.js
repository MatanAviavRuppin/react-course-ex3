export default class IngredientClass {

     constructor(name, image, calories, id) {
          this.name = name;
          this.image = image;
          this.calories = calories;
          this.id = id;
     }
}